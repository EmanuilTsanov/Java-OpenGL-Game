#version 330 core

in vec2 textureCoords;

uniform sampler2D textureSampler;
uniform vec3 inputColor;

out vec4 finalColor;

void main()
{
if(inputColor!=0)
	finalColor= vec4(inputColor, 1.0f);
	else
	finalColor = texture(textureSampler, textureCoords);
}