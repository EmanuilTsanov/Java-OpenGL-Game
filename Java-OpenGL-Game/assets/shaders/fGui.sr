#version 330 core

in vec2 textureCoords;

uniform sampler2D sampler;

out vec4 finalColor;

void main()
{
	finalColor = texture(sampler, textureCoords);
	if(finalColor.a < 0.5)
	{
		discard;
	}
}